<%= form_for @user, :url=>url_for(:action=>:create_user) do |f| %>
<%= f.label :email %>:<br/>
<%= f.text_field :email %><br/>

<%= f.label :permissions %>: <%= select :role, :level, [["Admin", 100], ["Editor", 50], ["User", 0]] %>

<br/>
<%= f.submit "Create" %>
<% end %>
