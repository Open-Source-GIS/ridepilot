<%= form_tag url_for(:action=>:vehicle) do %>

<%= date_select :report, :for_month, :discard_day=>true %><br/>

<%= collection_select :report, :vehicle_id, @vehicles, :id, :name %><br/>

<%= submit_tag "Report" %>
<% end %>
