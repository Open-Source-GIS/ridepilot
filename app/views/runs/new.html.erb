<div id="page-header">
  <h1>New Run: <%= @run.date.to_s(:long) %></h1>
</div>

<%= form_for @run do |f| %>
  <%= render "form", :f => f %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
